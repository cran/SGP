.onLoad <- function(lib,pkg) {
    require(splines)
    require(quantreg)
    require(grid)
    require(gtools)
    require(data.table)
    require(reshape)
    load(SGP)
}

export(
    "studentGrowthPercentiles",
    "studentGrowthProjections"
)
